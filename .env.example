# 企业微信机器人配置示例
# 复制此文件为 .env 并填入真实的配置值

# 企业微信群机器人Webhook URL（必需）
WEBHOOK_URL=https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=your_webhook_key

# 高德地图API密钥（可选，用于获取天气信息）
WEATHER_API_KEY=your_amap_api_key

# 天行数据API密钥（可选，用于获取老黄历运势信息）
# 申请地址：https://www.tianapi.com/
TIANAPI_KEY=your_tianapi_key

# 城市名称（可选，默认为上海）
CITY=上海

# Volces Engine ARK API配置（可选，用于生成AI内容）
ARK_API_KEY=your_ark_api_key
ARK_BASE_URL=https://ark.cn-beijing.volces.com/api/v3

# 定时任务配置（可选）
# Cron表达式格式：分 时 日 月 周
# 默认：每周一到周五上午10点执行
CRON_SCHEDULE=0 10 * * 1-5
